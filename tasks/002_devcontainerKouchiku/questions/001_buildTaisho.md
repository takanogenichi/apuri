# 質問: 「ビルド想定」の対象について

plan.md の確認事項1への回答「ビルド想定してください」について、Dockerfile に入れる
ビルドツールが大きく変わるため、具体的なビルド対象を確認させてください。

## 背景

- 001 のタスクでは「フランス語検定アプリ・Android 想定・コード言語は問わない」となっています。
- 一方、今回の DevContainer のベースは axolLoop に合わせ `node:20-bookworm`（Node.js）で進める方針です。
- Android ネイティブビルド（Gradle / Android SDK / JDK）を行うか、Web/JS のビルド（pnpm build）で
  済ませるかで、イメージに入れるツールが大きく異なります。

## 質問

以下のどれを想定していますか？（複数可・補足歓迎）

- [ ] A: Node.js / pnpm によるビルド（`pnpm build` 等）のみ。`node:20-bookworm` のまま、Android SDK は入れない。
- [ ] B: Android ネイティブビルド（Gradle + Android SDK + JDK）も DevContainer 内で行いたい。
      → この場合、ベースイメージや SDK 導入を別途設計する必要があります（イメージが大きくなります）。
- [ ] C: 現時点ではビルド対象未定。まずは Node.js ベースで雛形だけ用意し、ビルドツールは後追いで追加する。

## 回答欄

B案でお願いします。設計については私も知識が深いわけではないので、ある程度お任せ（おすすめで進めてもらって構いません）でお願いします。
あえていうのであれば、最も実装しやすい形で進めてもらって構いません。汎用的な形が実装しやすければそれで問題ございません。
ネイティブビルドができればそれで問題ないです。